module full_adder(input wire a,
       input wire b,
       input wire cin,
       output wire sum,
       output wire carry);

       wire o, p, x;
 assign o = a & b;
 assign p = a ^ b;
 assign sum = p ^ cin;
 assign x = p & cin;
 assign carry = o | x;

    endmodule
    
